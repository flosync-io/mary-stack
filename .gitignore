# Local Claude Code memory breadcrumb — the durable record is each block's progress.md
.claude/session-log.md
.claude/settings.local.json

# Live knowledge is stored in Supabase; commit only versioned snapshots you intend to pin
# knowledge/*.local.json

# generated review/session html
distill/out/
.DS_Store

# Python
.venv/
__pycache__/
*.pyc

# Secrets
.env
.env.*
