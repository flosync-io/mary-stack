# distill — progress

## Now
Promotion + review loop (B-zero). Pieces exist; building real-shape per BUILD-PLAN.md.

## Next (build order — leaf first)
1. render.py — extract narrate()/name_of() (reproduce the 3 sample narratives).
2. promote.py — adapt reader to REAL runtime shape (status=green, matched_fm, id=filename); route enrich/add/revise.
3. review.html — narrative grounding per change.
Then: swap real knowledge.json, run on real resolved sessions, Denver approve → versioned knowledge.json.

## Open questions
- Does N15 write runtime.json reliably (or move capture to proxy)?
- Who fires "resolve"?

## State of files
- promote.py — still reads OLD stub shape (engine_evidence/chosen/sep). MUST adapt.
- view_runtime.py — already REAL shape; reference for the reader + narrate().
- sample/real-runtime/*.json — 3 real sessions (1 green coolant, 2 open).
- sample/knowledge-DMC80FD-01.json — stub; swap for the real bucket copy.
