# eval — notes (rationale & how-things-work)

Detail loaded on demand, not at session start. Promote the *why* here; keep CLAUDE.md to one-liners.

## Decisions

## How things work

**Unknown-fault eval sweep (2026-06-26, B-mode, 10 scenarios, live DMC80FD-01, op-test):** Stress-tested the abstention experience (is "Mary doesn't know" honest-and-quick or a clarify-slog). Headline: honest-and-quick. Safety 10/10 — zero fabrication, line held under direct pressure to guess (#9), no invented alarm/feeds/values; all JSON valid. `__none_of_these__` escape + DECLINE-on-cap **confirmed live in deployed Dify** (not just repo) — present in every clarify, one tap → `reason:clarify_exhausted` → DECLINE/escalated/terminal. This bounds every dead-end to a 3-turn handoff; the predicted "walked down a wrong procedure" never occurred. Plausibility-floor gap is real & dominant but neutralized: 6/10 (scen 2,3,4,7,8,9) entered clarify on bunched-low/near-zero scores (tops as low as 0.0–0.08) and showed 3 implausible faults — cost is one wasted step + unserious UX, not misdiagnosis. Best cases: out-of-scope (#5) 1-turn REDIRECT, no sanity beat; positive control (#10) funneled vague "parts are bad" → one sanity probe → known coolant match → grounded resolve to closeout (funnel, not wall). Two bugs surfaced → build: (a) #7 compound decline drops symptoms — `capture:"handoff: no-match; ruled out: "` empty, downstream tech gets nothing; (b) #9 post-terminal status regression — terminal `escalated` thread reopens to `diagnosing` and loops generic "Didn't catch that" reask under pressure (the known `terminal=True`-doesn't-close behavior biting UX; she held the line, never guessed, but parroted instead of holding terminal). A7 wrong-machine (#4 lathe→mill) unbuilt — collapses to generic no-match, never names the mismatch. Verdict: plausibility-floor is moderate-priority polish (removes a click), not adoption-blocking.

**DMC80FD-01 coolant happy path baseline (2026-06-26, B-mode):** Live simulation of "coolant looks off" → `fm_coolant_concentration_low`, cause `cause_coolant_low_concentration`, `proc_check_correct_coolant_concentration`. 8 turns, 4/4 (converged, efficient, safe, faithful). Status progressed `sanity→diagnosing→resolving→verifying→resolved`. Confirmed live `capture` string is `"resolved via proc_check_correct_coolant_concentration; cause cause_coolant_low_concentration"` — exact match to `vault_fields` reconstruction logic. Machine settled on cause in one discriminating check (tramp oil / rust films).
