# 2026-06-25 — task list

Goals: (2) wire vault+memory for v5.1, (3) APIs compatible — IDs from tables + error codes, (4) everything in repo + one eval, (5) stretch 5.1→5.2. Backed by ADR-0001.

Critical path: **Repo → registry+errors → vault index → runtime verify → knowledge versioning → resolve→cache → commit → 1 eval → (stretch)**.
Coordination flag: anything touching `dify-proxy` is the live deploy (likely Saksham) — branch, don't hot-edit.
Ship-floor rule: **park reliably today; the async promotion job (v5.2 #8) stays stubbed.**

---

## Phase 0 — Pre-flight (10 min)
- [ ] `select count(*) from vault;` and `from machine_memory;` — confirm both 0 (reltuples lies; use count). 
- [ ] Confirm archives exist: `vault_archive_20260625`, `machine_memory_archive_20260625` (don't touch).
- [ ] List `mary-memory` bucket: confirm `knowledge/DMC80FD-01.json` exists; note whether any `runtime/DMC80FD-01/<conv>.json` objects exist for recent convs.
- [ ] `select * from information_schema.columns where table_name='vault';` — confirm `source_app` present.

## Phase 0.5 — Land the repo (20 min) → unblocks goal 4
- [ ] `git init` mary-stack; commit the skeleton.
- [ ] Move backend in: `backend/supabase/{functions,migrations}` (incl. `dify-proxy/`).
- [ ] Drop real `schema.json` into `contracts/`; drop `knowledge-DMC80FD-01.seed.json` into `knowledge/`.
- [ ] Commit ADR-0001 + this list under `docs/`.
- [ ] _Verify:_ `tests/test_engine.py` runs from the repo → 16/16.

## Phase 1 — Identity registry + error codes + repoints (goal 3) — foundation, ~1.5 hr
**Registries already exist** (`machines`, `operators` edge functions) — extend, don't rebuild.
- [ ] Extend `machines` table: `+ dify_app_key, knowledge_path, onboarded`. Seed `DMC80FD-01` (app key `1d5aed45-…`, `knowledge/DMC80FD-01.json`, onboarded=true). Reuse `operators` as-is.
- [ ] Reconcile operator key: `vault.user_id` → `operator_id` (the `operators`/`get_threads` canonical). One rename/alias.
- [ ] **Repoint `get-machine` BEFORE touching vault/machine_memory** (it reads `machines` + fat `vault` + `machine_memory.content`): keep its JSON shape; session detail from `runtime.json`; `memory_captured` ← `runtime.json.resolution_note`; drop the `machine_memory` join. _Verify:_ machine-detail payload unchanged in shape, sourced from new stores.
- [ ] In `dify-proxy`: resolve `machine`/`operator` against `machines`/`operators` before forwarding.
- [ ] Error contract (JSON envelope, not silent-empty): unknown machine → **404** `machine_not_onboarded`; unknown operator → **404/422** `operator_not_found`; bad/expired JWT → **401** `unauthenticated`; Dify non-2xx → **502** `upstream_error`.
- [ ] _Verify:_ bogus machine slug → 404 with code; valid op-test → 200; `get-machine/DMC80FD-01` still returns its shape.
- [ ] Note: `get-onboarded-knowledge`, `get-research-knowledge`, `get-video-knowledge` are **DEPRECATED** — ignore entirely. `knowledge.json` (via distill) is the only knowledge surface.

## Phase 2 — Vault → thin index (goal 2a) — ~45 min
- [ ] Migration: reduce `vault` usage to index columns `(conversation_id pk, machine_id, operator_id, status, source_app, created_at, updated_at)`. Drop reliance on `fault_type/steps_taken/ruled_out` (archived already).
- [ ] Confirm `threads` view + `get_threads(p_operator_id, …)` still resolve on the slimmed shape (title ← `machines.description`).
- [ ] _Verify:_ `select get_threads('op-test');` returns a well-formed `{threads:[…]}` (will be empty until capture writes rows — fine).

## Phase 3 — runtime.json capture: verify + repoint (goal 2b) — ~1 hr
- [ ] Confirm N15 ("Write runtime") fires every turn → object lands at `runtime/DMC80FD-01/<conv>.json`. Run one live turn, then list the bucket.
- [ ] If N15 writes: **repoint frontend history read** from `dify-proxy/messages` (live Dify) → `runtime/<machine>/<conv>.json` (Storage). _(coordinate w/ Saksham)_
- [ ] If N15 does NOT write reliably: add turn-capture in `dify-proxy` (blocking mode makes this clean) keyed by `conversation_id` (idempotent).
- [ ] On each turn, **upsert the vault index row** (status, updated_at) from the proxy — this is the `BACKEND-PROBLEMS.md` "message capture" solution.
- [ ] _Verify:_ run a 3-turn convo → `runtime/...json` has 3 turns AND `get_threads('op-test')` lists the convo with correct status.

## Phase 4 — knowledge.json versioning + rollback floor (goal 2c) — ~1 hr
- [ ] Stop in-place writes. Write builds as versioned objects: `knowledge/DMC80FD-01/<version>.json` with `_meta {version, parent_version, sha256}`.
- [ ] Add a `current` pointer (object or registry column) → the version Mary's N0 reads.
- [ ] Point N0 read at `current`.
- [ ] _Verify:_ publish v2, flip pointer, confirm N0 reads v2; flip back → reads v1 (rollback works, no data loss).
- [ ] Mirror `current` build to git on publish (diffable). _(can be a follow-up if time-boxed)_

## Phase 5 — resolve → cache + direct-reinforce (goal 2d) — ~1.5 hr
- [ ] Create `cache.json` (or `cache/<machine>.json`) shape: `{candidate_distillate, machine_id, source_conversation_id, op: add|supersede, matched_fm_id|null, ts}`.
- [ ] On resolve, read the engine's match result (`chosen`, `sep` vs `DECLINE_FLOOR=0.45`):
  - [ ] clean match to existing FM **and** resolution agrees → **direct**: increment that FM's evidence/recurrence + append `source_conversation_id` provenance to `knowledge.json` (a *new versioned build*, not in-place). **No content rewrite.**
  - [ ] match but contradicts → write supersede-candidate to `cache.json`.
  - [ ] novel / `chosen=null` / below floor → write new-FM candidate to `cache.json`.
- [ ] Stub the promotion job: leave cache entries un-promoted; do NOT build novelty/recurrence logic today.
- [ ] _Verify:_ replay a known DMC80FD-01 fault → direct reinforce (FM count +1, new build, pointer moves); replay a junk/novel case → lands in `cache.json`, `knowledge.json` untouched.

## Phase 6 — Commit + one eval case (goal 4) — ~45 min
- [ ] Commit all migrations + proxy changes + ADR + versioned-knowledge setup.
- [ ] Run **one** `mary-eval` case live against deployed v5.1 — pick a **memory-injection** case (proves wiring shows up), not a generic diagnosis.
- [ ] If it fails → emit a `finding` into `eval/findings/` with `fault_class` + the run manifest (`mary_yml v5.1 × knowledge <version> × app 1d5aed45…`).
- [ ] _Verify:_ eval runs end-to-end; finding (pass or fail) is recorded against the pinned tuple.
- [ ] `update memory` → promote today's decisions into block CLAUDE.md / progress.md.

## Phase 7 — STRETCH: 5.1 → 5.2 breaking changes (goal 5) — only if 0–6 clean
Pick the smallest first; each = test + re-run cited eval + version bump.
- [ ] **P0 no_answer** — add "not sure/haven't checked" to checks + `no_answer` to Interpret schema. _Verify:_ "dunno" must NOT clear a cause.
- [ ] **A7 wrong-machine redirect** — engine gate on `chosen=null`/all-low → redirect, never offer this machine's modes. _Verify:_ "stamping press jamming" → redirect.
- [ ] **A6 QA-flag** — param-change → "QA must approve, no numbers," distinct from safety STOP. _Verify:_ "nudge the feed down?" → QA flag, not STOP.
- [ ] Clarify plausibility-floor (bunched AND top ≥ floor; else DECLINE).
- [ ] Bump label to v5.2, re-pin manifest, update dashboard. **Breaking:** new `knowledge_version` + `mary_yml_version` → re-pin any open findings.

---

### Defer (not today)
Async promotion job (novelty/recurrence) · delta-log rollback (versioned-builds floor is enough for now) · `operator_memory` table-vs-JSON decision · analytical/SQL layer over cases · git-mirror automation if time-boxed.

### Two decisions to confirm before Phase 3/5
1. runtime.json writer: rely on N15, or move capture into the proxy? (Phase 3 forks on this.)
2. Resolve trigger: who fires "resolve" — Dify confirmed-path flag, or the proxy on status flip? (Phase 5 needs the hook point.)
